# 2026-08-14 — monorepo sync `eb267fe`

| | |
|--|--|
| Noted | 2026-08-14 |
| Remote | `eb267feff13129e568df38fb6fdf0ceb65f735d6` (2026-08-13 18:26 +0000) |
| Prev HEAD | `393430ee4934bc791b0d538f304a21691c517433` |
| xai-grok-shell | `1.0.3` (bumped from `0.2.121`) |
| Size | 7 commits · **807 files** · **+176507 / -117281** |
| PinkCode | open |

### Upstream changelog

**`afbc0fb`**

1. Guard in-process git status/diff from client spam
2. Tabbed usage/session-info modal
3. Session recaps follow the session language
4. Honor `startupHints` on session request metadata
5. Name latest Windows download `Grok Setup.exe`

**`8a14c91`**

1. Keep standalone worktree flag across cwd switch and resume
2. Make non-blocking startup structural
3. Drain subagents before session delete
4. Remove legacy managed MCP configs client

**`75e73f3` / `b13fa52`**

1. Include untracked files in HEAD→working git diff stats
2. `/rename` title cap, ghost-prefill, remote revert
3. Cap and pre-warm tokio blocking pools

**`be71313`**

1. Presence protocol end-to-end
2. Retain the displayed session model when the model catalog refreshes
3. Typed Automations tool-usage card variant
4. Restrict session directories to owner-only permissions

**`e5fd481`**

1. Capture and persist bound image capabilities
2. Wake the model when a UI kill never delivered a tool result
3. Advertise MCP protocol version 2025-11-25
4. Workspace EnsureBinding / MergeToMain / Push

**`eb267fe`**

1. **Make grok-4.6 the bundled default model**
2. Surface image capabilities on conversation metadata
3. Resolve Grok home from `USERPROFILE` on Windows
4. Preserve plan-mode prompt drafts across approval
5. Hooks: `updatedInput` on PreToolUse; tell hooks when the user stops a turn
6. Add web_search allowed/excluded domain configuration

### Notes

Grok 4.6 (`grok-4.6`) is now the bundled default for coding, web search, image describe, and session titles. It advertises `xhigh` plus high/medium/low, default **high**. Catalog broadcasts still carry each model's static default effort, not the session's chosen `xhigh`.

### PinkCode impact

| Area | Notes | Status |
|------|-------|--------|
| Default model / fallback catalog | Offline dropdown still listed 4.5 first | **done** |
| Catalog refresh vs session pick | `models/update` must not clobber current model or `xhigh` | **done** |
| Image capabilities | Conversation metadata + persist | open |
| Presence protocol | Gateway → client presence tiers | open |
| Hooks `updatedInput` / stop | PreToolUse + user-stop hook event | open |
| web_search domain allow/deny | Config surface | open |
| Plan-mode draft preserve | Keep composer draft across approval | open |

### Digested

- **When:** 2026-08-14
- **What:**
  1. Fallback model list leads with `grok-4.6`
  2. Session `reasoning_effort` lives on `ManagedAgentInfo`; `x.ai/models/update` only replaces the catalog
- **Paths:** `PromptBar.tsx`, `types.ts`, `liveTimeline.ts`, `agent_types.rs`, `agent_manager/models.rs`, `agent_manager.rs`, `acp/protocol.rs`
